# 报告字体

模板 v3 对齐 `INTERFACE.typ`：**SimSun（宋体）** 优先，Consolas 等宽字体用于代码。

## 编译

```powershell
cd docs
.\compile.ps1
```

直接使用系统字体，无需 `--font-path`。

## fonts/ 目录

`NotoSerifSC-*.otf` 与 `Inter_18pt-*.ttf` 为旧版模板遗留，当前 v3 不再使用，可手动删除。
