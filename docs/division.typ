// SmartMole Pro · 分工报告

#import "common.typ": *

#set document(
  title: "SmartMole Pro 分工报告",
  author: "张恒基",
  date: datetime.today(),
)

#show: report-init
#cover-page("分 工 报 告")

#front-matter[
  #abstract-block(
    [本报告明确 SmartMole Pro 各成员任务与#strong[实际执行状态]（截至 2026-06-08）。Wi-Fi 双板联机（versus）已由张耀辉与张恒基共同完成并验收；缪钰完成游戏 UI、WiFi 弹窗与音效/LED 基础框架；朱辰骏完成 `storage.c` 统计持久化（文件方案）。剩余超声波、WS2812B、排行榜 Tab、AI 难度等待补项由对应成员推进。],
    keywords: [分工报告；interface.h；三人并行；versus 冻结],
  )

  #pagebreak()
  #outline(title: outline-title, indent: 1.5em)
  #pagebreak()
]

#body-start

= 一、分工原则

+ *versus 冻结：* `MyWhackMole.c` 中双人联机（START / SCORE / FINISH）已验证，#strong[禁止重构] versus 核心代码；
+ *统一接入：* 新模块通过 `interface.h` 定义的结构体、枚举与回调挂钩；
+ *IP 外置：* 联机 IP 仅允许在宏 / 配置层修改，由张恒基负责；
+ *分支管理：* 每人 feature 分支 → 每日 push → 张恒基 review → merge main。

= 二、团队角色总览

#table(
  columns: (1.4cm, 1.6cm, 2.2cm, 2.2cm, 2.5cm, 1fr),
  inset: 5pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*角色*], [*成员*], [*学号*], [*班级*], [*模块*], [*当前状态*]),
  [统筹], [张恒基], [2024210926], [2024211301], [集成 + 联机联调 + WiFi UI], [✓ 已完成],
  [执行], [曹佳轩], [2024210994], [2024211312], [多模态输入 + 驱动], [⚠ 部分（K1）],
  [执行], [缪钰], [2024210996], [2024211303], [声光 + GUI + UI], [✓ 已完成（LED 降级）],
  [执行], [朱辰骏], [2024211039], [2024211301], [存储 + 排行榜 + 成就], [⚠ 骨架已建],
  [协助], [郭志罡], [2024210937], [2024211302], [关卡体系 + AI + 特殊地鼠], [✓ 关卡/特殊地鼠],
  [维护], [张耀辉], [2024211045], [2024211302], [Wi-Fi 联机 + versus], [✓ 已验收（与张恒基共建）],
)

= 三、已完成模块（维护态）

== 3.1 张耀辉 + 张恒基 — 双人对战 + Wi-Fi 联机（共建）

#callout(type: "success")[
  以下功能已在 MyWhackMole.c 中实现并验证，进入维护态。versus 核心由张耀辉实现，张恒基参与联调、双板实机测试与问题排查；后续仅配合 IP 宏位置确认，不做功能重构。
]

+ 双板 Wi-Fi 点对点联机对战（张耀辉 versus 实现 + 张恒基联调验证）；
+ START / SCORE / FINISH 联机同步；
+ MODE 单机闯关 / Wi-Fi 联机切换；
+ 与 LEVEL 1–5、COMBO、黄金 / 炸弹地鼠的联机兼容；
+ #strong[双板实机联调：]张恒基、张耀辉各出一块 DshanPI 开发板，同网段 ping 通后完成多轮对战测试；
+ #strong[联机调试：]排查 UDP 丢包、IP 配置、对局状态不同步、分数刷新延迟等问题，记录调试日志供答辩演示。

*已知限制：* 联机锁定两台固定板 IP；换板需改 IP 配置（张恒基外置，不改 versus 状态机）。

== 3.2 张恒基 — 联机阶段主要贡献（已完成）

+ 与张耀辉结对完成双板联机全流程验证，负责测试方案设计与问题复现；
+ 提供一块开发板作为联机对端，配合完成 START/SCORE/FINISH 同步测试；
+ 参与 versus 与 MODE/LEVEL 模块的集成联调，确认联机与闯关逻辑不冲突；
+ 整理联机演示流程与 IP 配置说明，为后续 IP 宏外置与答辩演示做准备。

== 3.3 已完成的公共功能

+ 五级 LEVEL 关卡系统（固定难度，待 AI 引擎扩展）；
+ 黄金地鼠（+5 分）、炸弹地鼠（-2 分）；
+ COMBO 连击奖励（郭志罡参与逻辑，已合入主线）。

== 3.4 缪钰 + 张恒基 — 音效与 GPIO LED（已完成 / 降级）

+ `sound_task` 独立线程（L334–350），击中播放 `hit.wav`；`res.zip` 中其余 7 个 wav 资源就位，待场景挂钩；
+ `led_task` GPIO LED 闪烁（L354–381），WS2812B 方案降级为板载 LED；
+ 游戏 UI：草地背景、锤子光标、9 洞、START/LEVEL/MODE/STATS/WIFI 按钮（L461–680）。

== 3.5 张恒基 — WiFi 图形连接（已完成）

+ `wifi_ui.c`：LVGL 弹窗输入 SSID/密码，底层 `wapi` + `ifup wlan0`；
+ 扫描结果保存 `/data/wifi_scan.txt`；已集成至游戏界面 WIFI 按钮。

== 3.6 朱辰骏 + 张恒基 — 统计数据存储（部分完成）

+ `storage.c`：读写 `/data/whackmole_stats.dat`，记录最高分、连击、联机胜率等；
+ 结算时 `storage_update_result` 自动写入（L850–854）；STATS 按钮弹窗展示；
+ 未实现：littlefs、Top-N 排行榜 Tab、成就判定 UI。

= 四、三人执行任务分工

== 4.1 曹佳轩 — 多模态输入 + 反转模式

*目标：* 完成开题「扩展目标」，不破坏触摸与 Wi-Fi 对战。

#table(
  columns: (0.7cm, 2.2cm, 2.5cm, 1fr),
  inset: 6pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*优先级*], [*任务*], [*交付物*], [*状态 / 约束*]),
  [P0], [物理按键 K1], [GPIO1 轮询触发 START], [✓ 已完成],
  [P0], [物理按键 K2], [右半区打击映射], [× 未做],
  [P0], [统一打击事件队列], [input\_queue.c + interface.h], [× 未做],
  [P0], [按键防抖与多事件], [短按/长按/连击识别], [× 未做],
  [P1], [HC-SR04 挥击检测], [100ms 距离突变滤波], [× 未做],
  [P1], [超声波驱动移植], [测距精度校准], [× 未做],
  [P2], [反转模式], [MODE\_REVERSE 独立模式], [× 未做],
  [P2], [输入精度校准 UI], [安装偏差补偿参数], [可持久化],
  [P3], [输入故障检测], [传感器超时/断线上报], [可降级纯触摸],
  [P3], [多模态输入联调], [与闯关/联机模式全场景测试], [配合张恒基集成],
)

*截止建议：* P0 第二周末；P1 第三周中；P2 第三周末。

== 4.2 缪钰 — 声光反馈 + UI 界面

*目标：* 完成开题「亮点目标」声光联动，补齐答辩 UI。

#table(
  columns: (0.7cm, 2.2cm, 2.5cm, 1fr),
  inset: 6pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*优先级*], [*任务*], [*交付物*], [*状态 / 约束*]),
  [P0], [音效系统], [sound\_task + hit.wav], [✓ 框架完成；7 wav 待挂钩],
  [P0], [音效资源整理], [res.zip 8 个 wav], [✓ 资源就位],
  [P1], [WS2812B 驱动], [SPI 时序灯环], [× 降级为 GPIO LED],
  [P0], [声光事件挂钩], [击中触发音效+LED], [✓ 击中路径已通],
  [P0], [LVGL 游戏 UI], [草地+9洞+按钮栏], [✓ 已完成],
  [P0], [排行榜单页面], [Tab 切换 Top-N], [× 由 STATS 弹窗代替],
  [P1], [界面视觉升级], [过渡动画/主题], [⚠ 部分（MiSans 字体）],
  [P1], [联机/结算 UI], [比分+Game Over 弹窗], [✓ 已完成],
  [P0], [WiFi 连接 UI], [wifi\_ui.c 弹窗], [✓ 张恒基已完成],
  [P2], [参数持久化], [亮度/音量/主题偏好], [对接朱辰骏 storage API],
  [P2], [全模块调试适配], [驱动兼容/模块联动/设备联调], [配合张恒基第三周集成],
  [P3], [外壳视觉], [灯条/按键布局 + 答辩展示], [第四周],
)

*截止建议：* P0 第二周末；P1 第三周中；P2—P3 第四周。

== 4.3 朱辰骏 — 数据存储 + 排行榜 / 成就

*目标：* 完成开题「亮点目标」本地持久化，支撑 AI 与 UI。

#table(
  columns: (0.7cm, 2.2cm, 2.5cm, 1fr),
  inset: 6pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*优先级*], [*任务*], [*交付物*], [*状态 / 约束*]),
  [P0], [统计数据封装], [storage.c 文件读写], [✓ 已完成],
  [P0], [结算数据写入], [FINISH 时 storage\_update\_result], [✓ 已对接],
  [P1], [排行榜数据 API], [Top-N 查询接口], [× 仅有 storage\_get\_data],
  [P1], [成就系统], [连击/通关/胜率], [× 未做],
  [P1], [littlefs 迁移], [替换 fopen 方案], [× 未做],
  [P2], [掉电保护与迁移], [备份/恢复 + 版本号校验], [防损坏],
  [P2], [成就展示挂钩], [与缪钰 UI 协作], [数据层/展示层分离],
  [P3], [数据可视化], [胜率/能力曲线/关卡完成度], [答辩加分],
  [P3], [存储压力测试], [频繁读写 + 并发场景], [配合集成联调],
  [P4], [Wi-Fi 上传], [云端同步], [可选],
)

*截止建议：* P0 第二周末；P1 第三周中；P2 第三周末。

== 4.4 未完成功能与答辩应对

#table(
  columns: (2.5cm, 1cm, 1fr),
  inset: 6pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*功能*], [*状态*], [*答辩话术*]),
  [HC-SR04 挥击], [×], [设计已完成，驱动调试中；当前触摸+K1 可完整演示],
  [WS2812B 灯环], [降级], [SPI 时序未调通，已用 GPIO LED 保证击中反馈],
  [排行榜 Tab], [×], [STATS 弹窗展示历史最佳与联机胜率，数据已持久化],
  [AI 动态难度], [×], [五级固定难度表已可玩，AI 为加分扩展],
  [versus IP 外置], [×], [双板联机已验证，换板改 L1103 宏即可],
  [8 音效全挂钩], [⚠], [框架+资源就绪，击中音效可现场演示，其余待扩展],
)

= 五、统筹与协助

== 5.1 张恒基 — 系统集成 + 联调

#table(
  columns: (0.7cm, 2.5cm, 1fr),
  inset: 6pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*优先级*], [*任务*], [*说明*]),
  [P0], [维护 interface.h], [结构体/枚举/回调规范],
  [P0], [versus IP 外置化], [宏/配置文件，不改 versus 状态机],
  [P0], [主菜单 + 模式框架], [单机闯关 / Wi-Fi 联机 / 选关入口],
  [P1], [第三周集成联调], [合并三分支，解决 mutex/栈/线程冲突],
  [P1], [全局容错], [外设异常/联机失败/断线重连提示],
  [P1], [联机演示脚本], [双板对战流程 + 答辩话术],
  [P2], [性能优化], [内存/延迟/帧率排查],
  [P2], [答辩演示脚本], [必做 + 加分演示顺序 + 备用方案],
)

== 5.2 郭志罡 — AI 动态难度（协助张恒基）

+ 维护 LEVEL 1–5 五关参数表，定义每关刷新间隔、停留时长、同屏数量、速度与特殊地鼠概率；
+ 完善炸弹、黄金地鼠与 COMBO 逻辑，与闯关/联机计分深度挂钩；
+ 设计各关难度曲线与通关条件，配合张恒基完成闯关流程联调；
+ 维护长度 10 的击打结果环形缓冲区，为 AI 引擎提供采样数据；
+ 每 5 秒计算命中率、反应时间、连击数 → 能力分（0—100），查表调节难度；
+ 可选：在关卡表基础上实现 AI 微调算法，支持固定难度 + 自适应双模式；
+ 统计各关通关数据与玩家表现，对接朱辰骏存储模块。

= 六、模块依赖与协作关系

#align(center)[
  #box(stroke: 0.5pt + gray-200, radius: 4pt, inset: 14pt, width: 100%)[
    #set text(size: 10pt)
    #strong[依赖流向] \
    曹佳轩（输入队列）→ 张恒基（游戏主循环） \
    缪钰（声光/UI）← COMBO / LEVEL / FINISH 事件 \
    朱辰骏（storage）→ 缪钰（读榜 UI）+ 郭志罡（关卡表） \
    张耀辉 + 张恒基（versus 共建）→ 冻结，张恒基负责 IP 外置与联机演示 \
    #v(0.5em)
    #strong[协作节点] \
    朱辰骏 ↔ 缪钰：storage.h 排行榜 API + 排行榜单页面 LVGL \
    郭志罡 ↔ 朱辰骏：关卡参数表格式 \
    曹佳轩 ↔ 张恒基：hit\_event\_t + mq 队列规范
  ]
]

= 七、代码合并约定

#table(
  columns: (2.5cm, 1fr),
  inset: 8pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*规则*], [*内容*]),
  [基准], [以当前 MyWhackMole.c 为准],
  [冻结], [versus / START / SCORE / FINISH 不重构],
  [接入], [新功能走 interface.h 事件回调],
  [分支], [feature/姓名-模块 → review → main],
  [联调], [第三周全员合并，张恒基解决冲突],
)

= 八、时间线与交付节点

#table(
  columns: (2.5cm, 1fr),
  inset: 8pt, stroke: 0.5pt + gray-200, fill: tbl-fill,
  table.header([*时间*], [*交付要求*]),
  [第 2 周后半], [曹：按键+队列 \| 缪：音效+GUI 主框架+排行榜页 \| 朱：littlefs+排行 API \| 张：interface.h + IP 外置],
  [第 3 周], [曹：HC-SR04 \| 缪：WS2812B+GUI 定稿 \| 朱：成就+排行 API 完善 \| 张+郭：AI 难度 \| 全员联调],
  [第 4 周], [反转模式 / 外壳 / Bug 修复 / 答辩视频 / 结题材料],
)

#card[
  *必做保底（5 月 4 日前）：* 按键 + 音效 + 排行榜 + WiFi 对战

  *加分冲刺（5 月 11 日前）：* 超声波 + WS2812B + 排行榜 + GUI 重构定稿
]

= 九、总结

本分工报告在 versus 模块（张耀辉 + 张恒基共建验收）基础上，更新了截至 2026-06-08 的实际执行状态：缪钰完成 UI/音效框架/GPIO LED；朱辰骏完成 storage 文件持久化；曹佳轩 K1 部分可用。超声波、WS2812B、排行榜 Tab、AI 难度与 IP 外置为后续待补项。详细实现分析见《结题报告》（`conclusion.typ`）。
