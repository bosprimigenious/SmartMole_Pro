# Typst / Markdown sources (kept locally, not tracked on remote)
*.typ
*.md

# Build artifacts
*.o
*.a
*.so
*.dep
Make.dep

# IDE / OS
.vscode/
.idea/
.claude/
Thumbs.db
Desktop.ini
*.swp
*~

# Large binaries (kept locally under firmware/ and archives/)
firmware/*.img
archives/*.tar
archives/*.tar.gz
archives/*.zip

# Typst compile scratch
docs/footer_test*.pdf
docs/_final_build/

# PPT / 文档构建临时目录
_pptx_extract/
docs/_from_md/
docs/ppt-projects/

# 本地打包与归档（不提交远程）
*.tar.gz
SmartMolePro_*_交付包*.zip
_ppt_review.txt
微信视频*.mp4

# 演示视频（本地保留并打入交付包，不进 git）
docs/videos/*.mp4
!docs/videos/README.txt

# Python
__pycache__/
*.pyc

# Fonts are tracked separately; ignore accidental downloads
docs/fonts/*.zip
